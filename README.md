# Android Backup Manager

A local, safety-critical web application for backing up personal files off
an Android phone via ADB, verifying every copy with SHA-256, and — as a
completely separate, explicitly-authorized step — deleting only the exact
files that were verified.

Full documentation is being built up alongside the implementation; see the
commit history for what's landed so far.
